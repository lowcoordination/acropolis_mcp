# Pinned, fully-resolved dependency versions for the environment this project is developed and
# tested against — generated via `pip list --format=freeze` in the project's own .venv.
#
# This is NOT a hash-locked file (no `--require-hashes` support) — no hash-capable resolver
# (uv, pip-tools) was available in this environment when this was generated, and installing one
# just to produce this file was out of scope for this cleanup pass. Regenerate with
# `pip freeze` (or switch to `uv lock` / `pip-compile --generate-hashes` if either becomes
# available) rather than hand-editing.
#
# `pyproject.toml`'s own `dependencies = [...]` list stays as the source of truth for minimum
# version *ranges* (what the package declares it needs); this file additionally pins the EXACT
# versions this project's own test suite has actually been run against, for reproducible CI runs
# and reproducible Docker builds.
#
# Regenerate: `.venv/bin/pip list --format=freeze | grep -v '^acropolis=='  > requirements-lock.txt`
# — then diff the result against this file and drop anything that crept in from ad hoc local
# `pip install`s (profilers, linters run by hand, etc.) rather than trusting the freeze verbatim;
# this file should only ever grow the packages this project's own dependencies actually need.

annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
asyncpg==0.31.0
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.0
click==8.4.2
cryptography==50.0.0
fastapi==0.141.1
google-re2==1.1.20251105
h11==0.16.0
httpcore==1.0.9
httpcore2==2.9.1
httptools==0.8.0
httpx==0.28.1
httpx-sse==0.4.3
httpx2==2.9.1
idna==3.18
iniconfig==2.3.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
mcp==1.29.0
mcp-types==2.0.0
opentelemetry-api==1.44.0
packaging==26.2
pip==26.0.1
pluggy==1.6.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pydantic-settings==2.14.2
Pygments==2.20.0
PyJWT==2.13.0
pytest==9.1.1
pytest-asyncio==1.4.0
python-dotenv==1.2.2
python-multipart==0.0.32
PyYAML==6.0.3
referencing==0.37.0
rpds-py==2026.6.3
sse-starlette==3.4.6
starlette==1.3.1
truststore==0.10.4
typing_extensions==4.16.0
typing-inspection==0.4.2
uvicorn==0.52.1
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.0.1
